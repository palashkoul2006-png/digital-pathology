flask>=2.3.0
werkzeug>=2.3.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=9.0.0
# HuggingFace Transformers + PyTorch (already installed from finetune step)
transformers>=4.35.0
torch>=2.0.0
# Load environment variables from .env file
python-dotenv>=1.0.0
# ── Acoustic Stethoscope module ───────────────────────────────────────────────
librosa>=0.10.0          # MFCC extraction from audio files
soundfile>=0.12.0        # Audio file I/O backend for librosa
tensorflow>=2.13.0       # CNN training and TFLite export
tensorflowjs>=4.10.0     # Convert model to TF.js for browser inference
# TensorFlow is optional — app runs in demo mode without it
# tensorflow>=2.13.0
